import Representation from './interface';
import { Files, Ranks, Squares, BoardSquareNumber } from './util/constants';

class RepresentationImpl implements Representation {
  constructor() {
    super();
  }
}

export default RepresentationImpl;
